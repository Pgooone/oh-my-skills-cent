# 「使用工作流」复用 Sync Plan

使用工作流 = 批量采纳 + 同步：把工作流引用的全部 Skill 经现有 Sync Plan（预览 → 确认 → 执行）写入目标 Agent，缺失的一并下载。不新造写入通道，延续「预览后写入」的安全模型。本决策于第一轮 grill 做出，2026-07-28 第二轮复核维持——Sync Plan 位于共享 Rust 核心，桌面壳与 Web 壳均可调用。
